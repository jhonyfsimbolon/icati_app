abstract class VerifyMemberView {
  onSuccessProvince(Map data);

  onFailProvince(Map data);

  onSuccessCity(Map data);

  onFailCity(Map data);

  onSuccessCompleteProfile(Map data);

  onFailCompleteProfile(Map data);

  onNetworkError();
}
